<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Category List</title>
    <link rel="stylesheet" href="/css/style.css" type="text/css" />
  </head>
  <body>
    <h1>Category List</h1>
    <ul>
      <!-- This code retrieves all category objects from the categoryController
     and prints them into the HTML site as a list -->
      <% for (let i = 0; i < categories.length; i++) { %>
      <li>
        <%= categories[i].name %>
        <a href="/category/edit/<%= categories[i].id %>">
          <button id="btn_<%= categories[i].name %>_edit">Edit</button>
        </a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
